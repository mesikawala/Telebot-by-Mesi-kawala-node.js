const commands = {
  greeting: /^!halo/,
  follow: /^!follow(.+)/,
  quote: /^!quote$/,
  news: /^!news$/,
  help: /^!help$/,
  introduction: /^!name$/,
  quake: /^!quake$/,
};

module.exports = commands;
